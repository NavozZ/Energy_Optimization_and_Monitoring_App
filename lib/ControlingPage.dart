import 'package:flutter/material.dart';

class Controlingpage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Control Panel'),
      ),
      body: Center(
        child: Text('Welcome to the Control!'),
      ),
    );
  }
}
